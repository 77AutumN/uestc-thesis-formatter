__pycache__/
*.pyc
*.pyo
output/
*.aux
*.log
*.synctex.gz
*.toc
*.lof
*.lot
*.bbl
*.blg
*.out
*.fls
*.fdb_latexmk
.DS_Store

# Legacy template tree (deprecated, replaced by vendor/DissertationUESTC/)
thesis-uestc/

# Cross-agent work artefacts (Gemini audit logs, etc.)
artifacts/

# Override and IPC dirs (private workflow only)
overrides/
.codex_ipc/
.gemini_ipc/
.pytest_cache/

# Build/cache
*.bcf
*.run.xml
*.nlo
*.synctex(busy)
*.xdv

# Local review/audit notes (not for upstream)
REVIEW_REPORT.md
PR_BODY.md

# Private redaction name list — loaded at runtime by tools/redact.py;
# CI injects from the REDACT_NAMES secret. NEVER commit.
.redact-names.local.txt
